import React from 'react'

const Description = (props) => {
    return (
        <div className="showcase-desc flex-column">
            <p>{props.txt}</p>

        </div>
    )
}

export default Description
